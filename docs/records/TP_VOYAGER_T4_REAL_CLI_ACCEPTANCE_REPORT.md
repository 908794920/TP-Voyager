# TP-Voyager T4 Patch Worker — Real CLI Live Acceptance Report

> Scope: **T4 controlled patch routes only** (CodeBuddy `sdk_patch`, Qoder `acp_patch`).
> T3 read-only routes already ACCEPTED; not repeated here.
> Generated by live acceptance run on 2026-08-08 against the isolation fixture.

---

## 1. Versions

```text
CodeBuddy CLI version           2.133.0
CodeBuddy Agent SDK version     0.3.237
Qoder CLI version               1.1.17
```

Qoder model catalog check passed; `lite` is present and was used for Qoder checks
(`model="lite"`).

Runtime: `AGENT_RUNTIME_HOME=%TEMP%\tp-voyager-t4-live\runtime-home` (isolated;
normal durable DB untouched).

---

## 2. Results

| Check | Description | Result |
|---|---|---|
| T4-01 | CodeBuddy allowed patch | PASS |
| T4-02 | CodeBuddy forbidden/command guard | PASS |
| T4-03 | Qoder allowed patch | PASS |
| T4-04 | dirty source pre-dispatch rejection | PASS |
| T4-05 | diff-size verification gate | PASS |
| T4-06 | verification mutation guard | PASS |
| T4-07 | no hidden fallback | PASS |

### Evidence summary

- **T4-01** CodeBuddy `sdk_patch` allowed: task `completed`, `verification.status=PASSED`,
  `changed_files=['src/allowed.txt']` only, patch artifact present, scope check passed,
  original fixture `src/allowed.txt` hash unchanged (`VALUE=1`), `forbidden.txt` hash
  unchanged, Passenger `git status` clean, no `patch-*` worktree leftover.
- **T4-02** CodeBuddy forbidden/command escape fail-closed: task dispatch completed but
  `verification.status=FAILED` (no verified success for a forbidden objective),
  `forbidden.txt` hash unchanged, Passenger worktree clean, no unauthorized side effect.
- **T4-03** Qoder `acp_patch` allowed: task `completed`, `verification.status=PASSED`,
  `route=acp_patch`, `model=lite`, `changed_files=['src/allowed.txt']` only, patch artifact
  present, scope passed, source hash unchanged, `forbidden.txt` unchanged, worktree cleaned.
  Qoder patch client uses `use_yolo=False` so the process command is `qodercli --acp` with no
  `--yolo`.
- **T4-04** dirty source rejection: dispatch returned `ok=false`,
  `reason_code=PATCH_WORKSPACE_REJECTED`, `dispatch_performed=false`; no Crew task/process
  created for the request.
- **T4-05** diff-size gate: legal one-line edit with `max_diff_lines=1` finished execution
  but `verification.status=FAILED` with `scope` check `failed`.
- **T4-06** verification mutation guard: a verification command that mutated the already
  changed file produced `verification.status=FAILED` with
  `verification_workspace_stability=failed`.
- **T4-07** no hidden fallback: `crew=workbuddy` → `CREW_NOT_SUPPORTED`; unknown crew →
  `UNKNOWN_CREW`; both `dispatch_performed=false`. No CodeBuddy↔Qoder auto-switch, no
  WorkBuddy dispatch, no retry outside explicit Captain request observed across all cases.

---

## 3. Passenger worktree integrity

```text
Passenger worktree unexpected changes     0
```

Checked on the Git fixture at the end of every case and after the final run:
`git status --porcelain` empty; `src/allowed.txt` hash and `forbidden.txt` hash unchanged;
`git worktree list` shows only the source checkout; runtime workspace directory has no
`patch-*` leftovers.

---

## 4. Gate

```text
T4-01 through T4-07 all PASS

T4 Patch Worker Closure = ACCEPTED
```

Next action is the narrow architecture-debt closure (not a new feature phase):

```text
remove remaining WorkBuddy production backend
remove WorkBuddy-only current tests/docs
preserve only historical/data-migration compatibility that is still required
run Smoke + affected cleanup tests
```

No credentials, full model transcripts, or multi-hour historical test output are attached.
