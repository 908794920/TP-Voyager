# v1.0.8 Read-Only Workspace Acceptance

## Release decision for this environment

**BLOCKED — Task 7 Windows vendor account-live acceptance was not executable in the current sandbox.**

This record distinguishes automated contract evidence from the release-blocking live matrix. No live row is promoted to PASS without a real Windows Qoder/CodeBuddy run.

## Captured environment

- Required live platform: Windows (per v1.0.8 final execution plan)
- Actual execution platform: Debian GNU/Linux 13 (`Linux 6.18.35`, x86_64)
- Python: `3.13.5`
- TP-Voyager commit at capture: `c17483ed84118026c17ef4ba98a88630a7cd9c0b`
- Branch: `upgrade/v1.0.8-workspace-readonly`
- Qoder CLI: **UNAVAILABLE** (`qodercli: command not found`)
- CodeBuddy CLI: **UNAVAILABLE** (`codebuddy` / `codebuddycli`: command not found)
- Python `mcp`: **UNAVAILABLE** (`ModuleNotFoundError`)
- Python `codebuddy_agent_sdk`: **UNAVAILABLE** (`ModuleNotFoundError`)
- Python `qoder_agent_sdk`: **UNAVAILABLE** (`ModuleNotFoundError`)
- Account-visible Qoder model: **NOT CAPTURABLE in this environment**
- Account-visible CodeBuddy model: **NOT CAPTURABLE in this environment**

The dependency installation attempt made during Task 0 was blocked by sandbox network/DNS restrictions. These environment limitations are not counted as product-code failures, but they do block the required real acceptance.

## Automated pre-live evidence

### Historical capacity replay

Command:

```text
python -m pytest tests/test_v108_workspace_read_only.py -q
```

Actual result:

```text
16 passed, 3 subtests passed
```

Covered evidence includes:

- normal read-only workspace dispatch at 32 / 64 / 150 / 220 / 300 / 1000 files without `read_scope`;
- >12 MiB workspace admission without `read_scope`;
- explicit bounded `read_scope` remains fail-closed;
- `repository_research` without required scope remains rejected;
- deterministic verification without its required contract remains rejected;
- Qoder explicit scope remains rooted in `routing_metadata.read_scope`, not `TaskLaunchRequest.allowed_paths`;
- CodeBuddy no-`context_id` workspace mode and explicit frozen-context mode remain distinct.

### Backend policy/unit evidence that does not require the missing server dependency

Command set:

```text
python -m pytest \
  tests/test_qoder_acp_client.py \
  tests/test_codebuddy_backend.py::CodeBuddyProbeTests \
  tests/test_codebuddy_backend.py::CodeBuddySdkClientTests \
  tests/test_codebuddy_backend.py::CodeBuddyBackendTests \
  tests/test_codebuddy_backend.py::CodeBuddyCaptainDispatchTests \
  tests/test_v108_workspace_read_only.py \
  -q
```

Actual result:

```text
42 passed, 3 subtests passed
```

This evidence verifies TP-Voyager-side command construction, host callback policy, dispatcher behavior, SDK option construction, and dual-mode routing. It is **not** a substitute for vendor runtime behavior.

## Qoder Windows Live Matrix

| Case | Required evidence | Result | Actual blocker |
|---|---|---|---|
| Q-LIVE-01 Broad repository research | Real cwd, no scope, native Grep/Glob/Read, cross-file evidence | **BLOCKED** | Required Windows + authenticated `qodercli` unavailable |
| Q-LIVE-02 Visible tool restriction | Bash/Edit/Write/Web/Agent not executable, proven by ACP/tool evidence | **BLOCKED** | No Qoder CLI/vendor session |
| Q-LIVE-03 Write/terminal invariant | Workspace unchanged; no successful terminal | **BLOCKED** | No Qoder CLI/vendor session |
| Q-LIVE-04 Sensitive paths | `.git`, `.qoder`, `.codebuddy`, `.env`, `*.pem` not leaked | **BLOCKED** | No Qoder CLI/vendor session |
| Q-LIVE-05 Explicit bounded snapshot | Real vendor session respects snapshot corpus | **BLOCKED** | No Qoder CLI/vendor session |
| Q-LIVE-06 Cancel/timeout/resume | Real durable lifecycle sample | **BLOCKED** | No Qoder CLI/vendor session |

**Qoder live verdict: BLOCKED.** Unit tests demonstrate the intended ACP host and startup policy, but cannot prove vendor-native tool behavior or sensitive-path enforcement.

## CodeBuddy Windows Live Matrix

| Case | Required evidence | Result | Actual blocker |
|---|---|---|---|
| C-LIVE-01 Broad repository research | No context/scope, native Read/Grep/Glob, no 256 KiB wall | **BLOCKED** | Required Windows + authenticated CodeBuddy SDK/CLI unavailable |
| C-LIVE-02 Read-only allow/deny | Read/Grep/Glob allowed; Edit/Write/Bash/Task/Web denied in real SDK session | **BLOCKED** | CodeBuddy SDK unavailable |
| C-LIVE-03 Sensitive paths | Runtime path policy blocks sensitive paths | **BLOCKED** | CodeBuddy SDK/vendor session unavailable |
| C-LIVE-04 Repository config isolation | `CODEBUDDY.md` / `.codebuddy` hostile config not trusted | **BLOCKED** | CodeBuddy SDK/vendor session unavailable |
| C-LIVE-05 Frozen Context regression | Explicit `context_id` stays `runtime_snapshot` | **BLOCKED** | CodeBuddy SDK/vendor session unavailable |
| C-LIVE-06 Cancel/resume | Resumed session preserves read-only policy | **BLOCKED** | CodeBuddy SDK/vendor session unavailable |

**CodeBuddy live verdict: BLOCKED.** The implementation deliberately does not promote `read_files` / `search_code` into CodeBuddy `controlled_capabilities` until this matrix passes.

A release-sensitive caveat remains to be proven live: the SDK policy is configured with a read-only native tool set and a `can_use_tool` path callback, but TP-Voyager unit tests cannot establish which vendor-native tool invocations require that callback in an authenticated runtime. The live matrix is therefore mandatory rather than ceremonial.

## Historical Failure Replay

| Historical case | Automated admission result | Required live result |
|---|---|---|
| 32 files | PASS | BLOCKED / NOT RUN |
| 64 files | PASS | BLOCKED / NOT RUN |
| 150 files | PASS | BLOCKED / NOT RUN |
| 220+ files | PASS | BLOCKED / NOT RUN |
| 12+ MiB | PASS | BLOCKED / NOT RUN |

The automated result proves repository size is no longer a normal pre-dispatch capacity limit when `read_scope` is omitted. It does not claim the vendor read the entire corpus, nor should it.

## TP-Spec Role Smoke / UltraPlan / UltraReview

The automated composition contract is recorded in `V1.0.8_TP_SPEC_ROLE_EXECUTION_ACCEPTANCE.md`.

The required real smoke set — Requirement research, Architecture broad analysis, Independent Review, `small_patch`, `verify_only`, Qoder/CodeBuddy participation, UltraPlan, and UltraReview — is **BLOCKED / NOT RUN** because the required vendor live environment is unavailable.

## Release-blocking conclusions

The following release gates are **not satisfied** in this environment:

1. Qoder Windows Live Acceptance.
2. CodeBuddy Windows Live Acceptance.
3. TP-Spec real role smoke including both crews.
4. Live sensitive-path evidence.
5. Live cancel/resume permission invariants.

Therefore this environment cannot issue `RELEASE v1.0.8`.

The implementation may proceed through Task 8 packaging/regression evidence, but the final release report must remain **RELEASE BLOCKED** until Task 7 is rerun and all required live rows are actual PASS.
